# react-simple-maps@3 still declares a React <=18 peer range but works fine with
# React 19 at runtime. Enable legacy peer resolution so `npm install` succeeds
# out of the box (one-click setup) without requiring extra CLI flags.
legacy-peer-deps=true
